import { useState } from "react";
import styled from "styled-components";

export default function PreferencesForm() {
  const [selectedLocations, setSelectedLocations] = useState([]);

  return (
    <>
      <p>Hello this is preferences</p>
    </>
  );
}
